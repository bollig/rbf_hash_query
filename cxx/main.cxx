#include <cstdlib>
#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;

int main (int argc, char** argv) {

	// load Matrix

	// Load nodes

	// hash nodes 

	// sort nodes according to hash (use std algorithm)

	// write new node order (index only)

	// write new node list (sorted coordinates)

	// write reordered matrix


	return 0;
}
